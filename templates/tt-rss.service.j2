[Unit]
Description=Tiny Tiny RSS feed updater
After=network.target
#After=mysql.service
After=postgresql.service

[Service]
User=www-data
ExecStart={{ ttrss_www }}/update_daemon2.php --quiet

[Install]
WantedBy=multi-user.target
